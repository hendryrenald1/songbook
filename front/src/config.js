import backgroundImage from "./img/red-grunge-concrete-texture-hd.jpg";

export const config = {
  style: {
    body: {
      // backgroundImage: "url(" + backgroundImage + ")",
      backgroundColor: "#F7F4E9",
    },
    a: {
      color: "blue",
    },
  },
};

export default config;
